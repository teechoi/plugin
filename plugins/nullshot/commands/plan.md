---
description: Turn the active Nullshot specification into a complete atomic task DAG.
---

Load `writing-nullshot-plans`. Inspect the repository, create stable outcome-sized tasks with expected files and verification, validate and review the graph, then replace the live plan with `upsert_jam_plan` using the current revision. Do not start execution.
